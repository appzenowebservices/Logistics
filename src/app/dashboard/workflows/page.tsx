import ComingSoonPage from "@/components/common/ComingSoonPage";

export default function WorkflowsPage() {
  return (
    <ComingSoonPage
      title="Workflow Builder"
      description="Visual workflow designer for customizing approval flows, booking processes, dispatch rules, and billing cycles without developers."
    />
  );
}
